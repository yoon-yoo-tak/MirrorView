import React from 'react';
import styled from 'styled-components';

const GrayWave = styled.div`
  width: 1450%;
  height: 7px;
  background: linear-gradient(90deg, transparent, #ccc, transparent);
`;


export default GrayWave;